// ============================================================
// version.js — Versi aplikasi
// ⭐ UBAH ANGKA DI SINI SAJA setiap kali rilis versi baru
// ============================================================

window.APP_VERSION = {
  version : '1.2.5',
  build   : '2026-09-24',
  name    : 'Expiry RTC',
  author  : 'Kevin Agustiana'
};

console.log(
  `📌 ${window.APP_VERSION.name} v${window.APP_VERSION.version} ` +
  `(build ${window.APP_VERSION.build})`
);
